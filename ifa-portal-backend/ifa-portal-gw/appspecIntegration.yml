version: 0.0
os: linux
files:
  - source: gw.war
    destination: /opt/tomcat/webapps
permissions:
  - object: /opt/tomcat/webapps/gw.war
    owner: tomcat
    group: tomcat
file_exists_behavior: OVERWRITE