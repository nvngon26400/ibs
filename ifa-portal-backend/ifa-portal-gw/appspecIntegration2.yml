version: 0.0
os: linux
files:
  - source: gw-2.war
    destination: /opt/tomcat/webapps
permissions:
  - object: /opt/tomcat/webapps/gw-2.war
    owner: tomcat
    group: tomcat
file_exists_behavior: OVERWRITE
